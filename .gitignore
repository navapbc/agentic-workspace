# Local, per-machine, and secret files — never commit.
# (The kit's own safety invariant: no secrets or absolute machine paths in the repo.)
bindings.env          # legacy (pre-0.2.0); per-machine paths now live in ~/.agentic-workspace/
.env
.env.*
*.local

# OS / editor metadata
.DS_Store
Icon?
*.swp
.idea/
.vscode/

# Dependencies / build output
node_modules/

# Note: reference/sandbox/ is intentionally tracked — it is a committed fixture
# regenerated by scripts/seed-sandbox.sh and checked by CI.
