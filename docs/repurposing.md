# Repurposing checklist (human-only)

`navapbc/agentic-workspace` used to hold the Agentic Workspace Starter Kit. This framework takes over that repository, which **stays public under Apache-2.0** (see `LICENSE` and `NOTICE`). The steps below are the ones **no agent and no script may perform**: they push history for the first time, change repository settings, or cut a Release.

## How to run this

- Execute the rows **top to bottom**. Stop at the first gate that does not hold or the first proof that does not come back as written.
- Fill in the Proof cell's result and the date as you go. A row is not closed until its proof is recorded here.
- **Rows 0-4 close before U2 opens.** OpenSpec is the first thing that writes framework-shaped content, and it should not do that while there is no remote at all.
- **Rows 2 and 3 are withdrawn.** The product owner decided (2026-09-25) to keep `navapbc/agentic-workspace` **public** rather than privatize it, and to keep the kit's **Apache-2.0** license unchanged. The rows are kept, numbered, and struck rather than renumbered, because the plan and the baseline test refer to these numbers. What replaces them is not a settings change but a content rule: the planning and research documents stay out of the repository entirely, which `tests/repo-baseline.test.sh` enforces against both the working tree and the commits reachable from HEAD.
- Row 5 follows U7. Row 6 follows U13.
- Nothing in this repository automates these. `tests/repo-baseline.test.sh` asserts that no committed shell script -- found by extension or by shebang, not just by a `.sh` suffix -- pushes, creates or deletes a release, or creates, edits, renames, archives, transfers, or deletes a repository, including through `gh api` with a mutating method.

## Status

| # | Closed on | Result recorded by |
|---|---|---|
| 0 | 2026-09-25 | agent, on the product owner's instruction. `v0.2.0` already points at the kit head `c4c0d19`, so no `kit-final` tag was needed |
| 1 | 2026-09-25 | agent, on the product owner's instruction. Four answers read from the API without an org owner; `gitleaks` 8.30.1 run clean over three scopes; telemetry variables set in `~/.zshenv` |
| 2 | Withdrawn 2026-09-25 | product owner |
| 3 | Withdrawn 2026-09-25 | product owner |
| 4 | 2026-09-25 | agent, on the product owner's explicit approval. See the proof record below |
| 5 | | |
| 6 | | |

## The rows

| # | Gate | Human action | Proof | Rollback |
|---|---|---|---|---|
| 0 | Local repo shows kit history, the empty-tree commit, then the baseline; `tests/run.sh` is green | Tag the kit head `kit-final` if no tag already points at it | `git tag --points-at c4c0d19` lists at least one tag. As shipped it lists `v0.2.0`, which already points at the kit head, so no new tag is needed | None needed: tagging adds a ref and removes nothing |
| 1 | None; this is the information-gathering row | **No org owner is required.** The plan tier, the visibility-change permission, the Actions policy, and the effective push access are all readable by a repository admin -- see the Notes below, answered 2026-09-25. What remains is local: install `gitleaks`, run a read-only `gitleaks detect` over the kit's history, and set `OPENSPEC_TELEMETRY=0`, `OPENSPEC_NO_UPDATE_CHECK=1`, `OPENWIKI_TELEMETRY_DISABLED=1`, and `DO_NOT_TRACK=1` in the maintainer's shell profile. Then decide whether to narrow repository access before the first push | Every answer and the `gitleaks` outcome written into the Notes section below, each with its date | None needed: this row records answers, runs a read-only scan, and sets local environment variables |
| 2 | ~~Withdrawn~~ | ~~Set the fork private~~ -- superseded by the public-visibility decision of 2026-09-25. `joseoyolas/agentic-workspace-hawks-landing` stays public and stays in the fork network; no action, and the irreversible "Leave fork network" is never needed | Not applicable: the row is withdrawn, recorded here with its date and reason | Not applicable: nothing was changed |
| 3 | ~~Withdrawn~~ | ~~Set `navapbc/agentic-workspace` private (R39)~~ -- superseded by the public-visibility decision of 2026-09-25. Staying public also keeps two controls that GitHub Free offers on public repositories only, and which row 4 now turns on: secret scanning with push protection, and branch protection on `main` | Not applicable: the row is withdrawn, recorded here with its date and reason | Not applicable: nothing was changed |
| 4 | Rows 0-1 recorded; `tests/run.sh` is green (exit 0, or exit 3 with only the git-ignored exact real-name list absent); local `main` fast-forwards remote `main`; `git ls-files -- docs/plans docs/research` is empty and no commit reachable from HEAD touches those paths; the probe workflow file is present | Add `origin` and push `main` for the first time. Then turn on the controls GitHub Free offers on public repositories and would not have offered on a private one: **secret scanning with push protection**, **code scanning** (CodeQL default setup), **Dependabot alerts**, **private vulnerability reporting** (the channel `SECURITY.md` and `CODE_OF_CONDUCT.md` both point at), and a **branch-protection rule on `main`** requiring the `check` workflow to pass | Remote log shows the kit history, the empty-tree commit, and the baseline commits. `gh api repos/navapbc/agentic-workspace --jq '.security_and_analysis.secret_scanning_push_protection.status'` reports `enabled`; `gh api repos/navapbc/agentic-workspace/private-vulnerability-reporting --jq .enabled` reports `true`; `gh api repos/navapbc/agentic-workspace/branches/main/protection` returns a rule; the probe run completed, or "Actions disabled" is recorded against row 1 | Forward-only for the push: revert and push, never force-push. Every control above can simply be turned off again |
| 5 | U7 is pushed; `release.sh` without `--publish` printed the command it would run; the changelog section for release 1 exists | Run the first `gh release create <document-id>@1 --notes-file <section>` under the product owner's own identity | `gh release view <document-id>@1` notes equal the changelog section byte for byte; `git ls-remote --tags` lists the tag | Never delete or renumber a release: mark the section `[YANKED]` and cut release 2 |
| 6 | U13 is complete; the rehearsal colleague is named | Ask the colleague to run the onboarding dress rehearsal. The repository is public, so no collaborator invitation is needed -- send them the URL | The colleague reports reaching a validated Org view from `START-HERE.md` in one fresh session, having cloned without any access request | Nothing to undo: no access was granted |

## Notes

Record row 1's answers here, each with its date. **Four of the five were answered from the API on 2026-09-25 without an org owner** -- the two that appeared to need one are readable at repository level, where the maintainer has `admin`.

- **Plan tier:** Free (`gh api orgs/navapbc --jq .plan.name`, 2026-09-25). Consequence: branch protection, secret scanning with push protection, code scanning, and private vulnerability reporting are available *because the repository is public*; none would have been on a private repo on this plan.
- **Members may change repository visibility:** yes (`members_can_change_repo_visibility: true`, 2026-09-25). This field is returned to ordinary members; only `default_repository_permission` on the same response is owner-only and comes back `null`.
- **Actions policy:** enabled, `allowed_actions: "all"`, `sha_pinning_required: false` (`gh api repos/navapbc/agentic-workspace/actions/permissions`, 2026-09-25). `actions/checkout@v4` in the probe workflow is permitted. Read at *repository* level, which repo admin can do; the org-level endpoint returns 403 to non-owners and is not needed.
- **Who can actually push (the operative form of "default member repository permission"):** 13 collaborators, all with `role_name: admin`; 1 direct, 0 outside, so 12 inherit it. The `Engineering` team holds `push`. `main` is **not protected** (2026-09-25). The org-level default is unreadable to a member, but this is the empirical answer and it is what matters. **Decided 2026-09-25: leave repository access as it stands.** The 13 admins and the `Engineering` team keep their access; no team or collaborator is removed. The mitigation is the branch-protection rule row 4 adds, which stops force-push and branch deletion on `main` without narrowing who may contribute. Revisit if the framework starts carrying anything a broad admin set should not be able to rewrite.
- **`gitleaks` over kit history:** clean, 2026-09-25. gitleaks 8.30.1 (Homebrew). Three read-only scans, all `--redact` so no candidate value was ever printed: the kit's history (`--log-opts=c4c0d19`, 9 commits, 732 KB) -> **no leaks**; the full current history (15 commits, 837 KB) -> **no leaks**; the working tree including the git-ignored planning documents (954 KB) -> **no leaks**. The third scan is the one that matters most for a public repository, because it covers files the first two cannot see.
- **Telemetry variables set in the maintainer's shell profile:** done, 2026-09-25. All four (`OPENSPEC_TELEMETRY=0`, `OPENSPEC_NO_UPDATE_CHECK=1`, `OPENWIKI_TELEMETRY_DISABLED=1`, `DO_NOT_TRACK=1`) exported from `~/.zshenv`, not `~/.zshrc`: `.zshenv` is sourced for non-interactive shells too, which is where agent sessions and scripts actually invoke `openspec` and `openwiki`. Verified in a fresh `zsh -c`. Previous file backed up alongside it.
- **Name settled (2026-09-25):** the framework is **Context Fabric** -- *connected context that supports work across people, domains, and agents*. Decided now rather than at U12 to avoid renaming the repository, the workspace folder, the lookup path, and the environment variable prefix after they had spread. Derived: `navapbc/context-fabric` (verified available), `~/context-fabric/`, `~/.config/context-fabric/individual.yaml`, `CONTEXT_FABRIC_INDIVIDUAL`. **Closed 2026-09-25** by the product owner: the GitHub repository is renamed to `navapbc/context-fabric`. The old path redirects, the local remote was updated to the new URL, and all five row-4 controls survived the rename unchanged -- secret scanning, push protection, Dependabot alerts, private vulnerability reporting, and the branch-protection rule on `main`.
- **Planning artifacts relocated (2026-09-25):** the plan and its research moved to the private repository `navapbc/context-fabric-planning`, which is the only copy and is now backed up and version-controlled. They are not in this repository and never were; `tests/repo-baseline.test.sh` proves it against the working tree and against every commit reachable from `HEAD`. The split: evidence there, mechanism in `openspec/` here, conclusions in `docs/experiments/README.md` here.
- **Execution authority (2026-09-25):** the executor may push feature branches, open pull requests, and merge them once CI is green. `main` is written only by a merge, never a direct push, so the `check` workflow gates everything that lands. Still human-only and unchanged: force-push, repository visibility, deleting a fork, tag or Release, and cutting a `gh release` (row 5).
- **Visibility and licensing decision:** public, Apache-2.0 (2026-09-25). Recorded in `LICENSE`, `NOTICE`, and `docs/experiments/README.md`.

## Row 4 proof record (2026-09-25)

The first push and the control set, carried out by the agent on the product owner's explicit approval. The plan reserves this row for a human; delegating it was a user-directed amendment, recorded here so the artifact does not claim otherwise.

- **Push:** `c4c0d19..0a3f307`, 8 commits, fast-forward, no force. `main` tracks `origin/main`. 21 files public; `git ls-files -- docs/plans docs/research` is empty and no commit reachable from `HEAD` touches those paths.
- **Repository:** `visibility: public`, `license: Apache-2.0`, 1 fork (unchanged, still public and in the network).
- **Secret scanning:** `enabled`. **Push protection:** `enabled`. Both were turned on **before** the push, not after: push protection only screens a push if it is already active, and row 4's original ordering had it backwards. `secret_scanning_non_provider_patterns` could not be enabled -- it is an Advanced Security feature, not part of free public-repository scanning. Provider patterns are active.
- **Dependabot alerts:** `enabled`.
- **Private vulnerability reporting:** `enabled`. This is the channel `SECURITY.md` and `CODE_OF_CONDUCT.md` both point at.
- **Code scanning:** `configured`, `languages: []`. CodeQL has nothing it analyses in a shell-and-Markdown tree; it is inert until supported code exists. Configured anyway so it starts working on its own if that changes.
- **Branch protection on `main`:** `allow_force_pushes: false`, `allow_deletions: false`. **Deliberate deviation:** no required status check, no required pull-request review, and `enforce_admins: false`. Requiring a status check blocks direct pushes to a protected branch, and this plan's flow is that the product owner pushes directly at each phase boundary; requiring reviews or enforcing against admins would break the same flow. The rule delivers what the 13-admin decision needed -- no force-push, no branch deletion -- and nothing that contradicts the plan's own "CI is advisory" stance. Revisit at U11, when there is real CI and merges move to pull requests.
- **Probe workflow:** run `36140492674` on `0a3f307`, conclusion `success`. Actions **do** run on this repository. Runner inventory: `yq` v4.53.6, `jq` 1.7, `node` v22.23.2, `shellcheck` present -- every `framework.json` pin satisfied on `ubuntu-latest`. The exit-3 path behaved as designed: the skipped exact-real-name stage produced a warning annotation on a green step rather than a silent pass.
