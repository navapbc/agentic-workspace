{
  "_comment": "SYSTEM RESOURCE template. Describes a shared system and how to reach it. NO credentials, NO product ownership. Remove all _comment fields before saving.",
  "kind": "systemResource",
  "id": "system:{{system-slug}}",
  "schemaVersion": "1.0.0",
  "updatedAt": "{{YYYY-MM-DDTHH:MM:SSZ}}",
  "name": "{{Human-readable system name}}",
  "description": "{{What this system is, in one or two sentences.}}",
  "interfaces": [
    {
      "type": "{{web | api | git | issue-tracker | data-warehouse}}",
      "url": "https://{{host}}/{{path}}",
      "_comment": "No embedded credentials in URLs. No absolute local paths."
    }
  ],
  "provenance": {
    "source": "{{where this fact came from}}",
    "addedBy": "{{name}}"
  },
  "validation": {
    "validatedBy": "{{name}}",
    "validatedAt": "{{YYYY-MM-DDTHH:MM:SSZ}}"
  }
}
