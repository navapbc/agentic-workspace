{
  "_comment": "PRODUCT PROFILE template. Binds a product to the systems and repos it uses, and layers product-specific meaning on top of shared records. Referenced by the product area's AGENTS.md via 'Context Fabric profile: product:<slug>'. Remove all _comment fields before saving.",
  "kind": "productProfile",
  "id": "product:{{product-slug}}",
  "schemaVersion": "1.0.0",
  "updatedAt": "{{YYYY-MM-DDTHH:MM:SSZ}}",
  "name": "{{Product name}}",
  "description": "{{What the product is, in one or two sentences.}}",
  "aliases": ["{{friendly-name}}"],
  "maintainers": [
    { "name": "{{steward name}}", "role": "product steward" }
  ],
  "systems": [
    {
      "systemId": "system:{{system-slug}}",
      "anchors": [
        {
          "_comment": "An exact, validated home inside the system: a Jira project key, a Confluence page ID, a folder.",
          "type": "{{jira-project | confluence-page | folder}}",
          "value": "{{EXACT-IDENTIFIER}}"
        }
      ]
    }
  ],
  "repositories": [
    {
      "resourceId": "repository:{{org}}/{{repo}}",
      "alias": "{{product-local friendly name}}",
      "selectionTier": "{{primary | supporting | conditional}}",
      "coverage": "{{direct | reference}}",
      "useWhen": "{{the trigger that makes this repo relevant to a task}}",
      "pathScope": ["{{optional glob to narrow to the parts this product owns}}"]
    }
  ],
  "taskGuidance": {
    "synthesisBelongsIn": "product-work/{{product-slug}}/docs",
    "notes": "{{Where outputs for this product go, and any product-specific routing.}}"
  },
  "provenance": {
    "source": "{{where these facts came from}}",
    "addedBy": "{{name}}"
  },
  "validation": {
    "validatedBy": "{{name}}",
    "validatedAt": "{{YYYY-MM-DDTHH:MM:SSZ}}"
  }
}
