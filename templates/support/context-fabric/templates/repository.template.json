{
  "_comment": "REPOSITORY RESOURCE template. Identity and checkout facts for ONE repository, defined once even when many products use it. Product-specific meaning belongs in profiles, not here. Save to records/resources/repositories/<org>/<repo>.json. Remove all _comment fields before saving.",
  "kind": "repositoryResource",
  "id": "repository:{{org}}/{{repo}}",
  "schemaVersion": "1.0.0",
  "updatedAt": "{{YYYY-MM-DD}}",
  "contentLocations": [
    { "path": "records/resources/repositories/{{org}}/{{repo}}.json" }
  ],
  "organization": "{{org}}",
  "repository": "{{repo}}",
  "system": "system:{{hosting-system-slug}}",
  "url": "https://{{host}}/{{org}}/{{repo}}",
  "description": "{{What this repository holds, in one sentence.}}",
  "checkoutDirectory": "{{repo}}",
  "lifecycle": "{{active | anticipated | unavailable | deprecated | retired | excluded}}",
  "accessState": "{{available | request-required | unavailable}}",
  "_comment_syncable": "syncable: true requires lifecycle active + accessState available + validation.status validated. The lint enforces this, so 'we sync this' cannot be claimed about a repository nobody has confirmed is reachable.",
  "syncable": false,
  "stewardship": {
    "owner": "{{team or person who owns this repository}}",
    "note": "{{anything a consumer must know before relying on it}}"
  },
  "provenance": {
    "source": "{{where this fact came from}}",
    "addedBy": "{{name}}"
  },
  "validation": {
    "status": "{{validated | unvalidated | disputed}}",
    "validatedBy": "{{name}}",
    "validatedAt": "{{YYYY-MM-DD}}"
  }
}
