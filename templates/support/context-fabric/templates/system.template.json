{
  "_comment": "SYSTEM RESOURCE template. Describes a shared system your products touch and how to reach it. NO credentials, NO product ownership. Save to records/systems/<slug>.json. Remove all _comment fields before saving.",
  "kind": "systemResource",
  "id": "system:{{system-slug}}",
  "schemaVersion": "1.0.0",
  "updatedAt": "{{YYYY-MM-DD}}",
  "contentLocations": [
    { "path": "records/systems/{{system-slug}}.json" }
  ],
  "name": "{{Human-readable system name}}",
  "description": "{{What this system is, in one or two sentences.}}",
  "ownership": {
    "owner": "{{team or org that runs it}}",
    "contact": "{{how to reach them — a channel or role, not a personal credential}}"
  },
  "interfaces": [
    {
      "_comment": "One entry per way in. No embedded credentials in URLs, no absolute local paths. validationStatus records whether a person actually confirmed this interface works, and when — an unvalidated interface is a lead, not a fact.",
      "type": "{{web | api | cli | git | issue-tracker | wiki | data-warehouse | other}}",
      "url": "https://{{host}}/{{path}}",
      "accessGuidance": "{{how to obtain access — never the credential itself}}",
      "validationStatus": "{{validated | unvalidated | disputed}}"
    }
  ],
  "provenance": {
    "source": "{{where this fact came from}}",
    "addedBy": "{{name}}"
  },
  "validation": {
    "status": "{{validated | unvalidated | disputed}}",
    "validatedBy": "{{name}}",
    "validatedAt": "{{YYYY-MM-DD}}"
  }
}
