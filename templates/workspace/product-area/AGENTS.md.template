# {{PRODUCT_AREA}} -- Agent Instructions

<!--
  Thin, product-area guidance. Boundaries and routing only.
  The FIRST line of substance is the Context Fabric profile declaration below.
  For Claude Code users, add a CLAUDE.md here containing just: @AGENTS.md
-->

Context Fabric profile: product:{{product-slug}}

Steward: {{name}}

## Boundaries

- {{What this product area covers, and what it does not.}}
- This is {{a strategy/documentation workspace | a workspace that also holds runnable prototypes}}; {{note the distinction if relevant}}.
- Product-specific guidance here wins when it is more precise than the parent, unless it conflicts with the workspace authority boundary.

## Where things live

- Reusable procedures: the support layer's `skills/`. Do not duplicate them here.
- Shared facts about this product's systems and repos: resolve `product:{{product-slug}}` in the Context Fabric.
- Synthesis and deliverables: this area's `docs/` lanes and `Deliverables/` (if used).

## Local conventions

- {{Stable identifiers, naming schemes, or vocabulary specific to this product. Keep short; reference a memory or Context Fabric record for detail.}}
