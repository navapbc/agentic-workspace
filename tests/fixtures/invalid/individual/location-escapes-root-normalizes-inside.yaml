# LOCATION_ESCAPES_ROOT -- this one normalizes back inside the tree and is
# rejected anyway. The rule is no upward segment at all, not no net escape: a
# normalizing rule asks every reviewer to redo the arithmetic, and one of them
# eventually gets it wrong.
id: example-practitioner
kind: individual
schema_version: 1
bindings:
  - ref:
      id: example-claims-context
      release: 1
      location: file:documents/org/../bounded-context/example-claims-context.yaml
    documents_root: /Users/name/context-fabric-documents
    framework_root: /Users/name/context-fabric
    output_root: /Users/name/context-fabric-documents/views
    harness:
      id: example-harness
