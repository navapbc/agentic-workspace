# REQUIRED_KEY_MISSING -- `release` is absent. An Org document without a release
# number cannot be compared to the copy a Bounded Context recorded, so the
# reference machinery has nothing to work with.
id: example-agency
kind: org
schema_version: 1
organization:
  id: example-agency
  name: Example Agency
systems:
  - id: claims-warehouse
    name: Claims Warehouse
    kind: data-warehouse
    status: active
    interfaces:
      - id: read-api
        status: active
        type: rest
        urls:
          - https://api.example.invalid/claims/v1
        auth:
          method: oauth
          env: {}
        limitations: []
